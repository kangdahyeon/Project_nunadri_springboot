<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.springproject.mapper.CommunityCommentMapper">


   <insert id="insertCommunityComment">
      INSERT INTO COMMUNITY_COMMENT
      (
	     	COMMUNITY_COMMENT_NO
	      , NOTICE_CATEGORY
	      , NOTICE_NO
	      , NICKNAME
	      , COMMUNITY_COMMENT
	      , COMMUNITY_COMMENT_REGDATE

      ) values (
      (SELECT
      IFNULL(MAX(COMMUNITY_COMMENT_NO), 0) + 1
      FROM
      		COMMUNITY_COMMENT ALIAS_FOR_SUBQUERY
      WHERE
      		NOTICE_CATEGORY =#{noticeCategory}
      AND
      		NOTICE_NO = #{noticeNo})
      , #{noticeCategory}
      , #{noticeNo}
      , #{nickname}
      , #{communityComment}
      , NOW()
      )
   </insert>

   <select id="getCommunityComment" resultType="CommunityCommentVO"> 
      SELECT * FROM COMMUNITY_COMMENT
      WHERE
      		NOTICE_CATEGORY =#{noticeCategory}
      AND
      		NOTICE_NO = #{noticeNo}
      ORDER BY COMMUNITY_COMMENT_NO ASC
      </select>
   
   <delete id="deleteCommunityComment">
      DELETE FROM COMMUNITY_COMMENT
      WHERE
      		COMMUNITY_COMMENT_NO = #{commentNo}
      AND 
      		NOTICE_CATEGORY =#{noticeCategory}
      AND 
      		NOTICE_NO = #{noticeNo}
   </delete>

</mapper>